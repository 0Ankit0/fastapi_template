# Project identity
PROJECT_NAME=Project Template
BASE_URL=http://127.0.0.1:8000/api/v1
IOS_BUNDLE_ID=com.example.projectTemplate

# Analytics
# This file is intentionally versioned because the mobile app only stores
# startup-safe defaults here. Keep secrets in backend/.env or your secret store.
ANALYTICS_ENABLED=false
ANALYTICS_PROVIDER=posthog
POSTHOG_API_KEY=
POSTHOG_HOST=https://us.i.posthog.com
MIXPANEL_PROJECT_TOKEN=
MIXPANEL_API_HOST=https://api.mixpanel.com
