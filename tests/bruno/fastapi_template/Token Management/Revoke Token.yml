info:
  name: Revoke Token
  type: http
  seq: 2

http:
  method: POST
  url: /api/v1/token-management/revoke/{token_id}
  params:
    - name: token_id
      value: ""
      type: path
      description: undefined
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
