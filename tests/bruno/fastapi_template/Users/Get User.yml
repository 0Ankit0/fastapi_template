info:
  name: Get User
  type: http
  seq: 5

http:
  method: GET
  url: /api/v1/users/{user_id}
  params:
    - name: user_id
      value: ""
      type: path
      description: undefined
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
