info:
  name: List Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URI}}/api/v1/users/limit=10"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
