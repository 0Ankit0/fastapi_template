info:
  name: Inherit Role
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URI}}/api/v1/organizations/:org/rbac/role-inheritance"
  params:
    - name: org
      value: testorg
      type: path
  body:
    type: json
    data: |-
      {
        "role": "admin",
        "parent_role": "member"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: success
    request:
      url: "{{BASE_URI}}/api/v1/organizations/:org/rbac/role-inheritance"
      method: POST
      params:
        - name: org
          value: testorg
          type: path
      body:
        type: json
        data: |-
          {
          	"role": "admin",
          	"parent_role": "member"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: date
          value: Mon, 22 Jun 2026 04:30:22 GMT
        - name: server
          value: uvicorn
        - name: content-length
          value: "62"
        - name: content-type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "Role inheritance added successfully.",
            "data": null
          }
