info:
  name: Remove Permission
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{BASE_URI}}/api/v1/organizations/:org/rbac/permissions"
  params:
    - name: org
      value: testorg
      type: path
  body:
    type: json
    data: |-
      {
        "role": "member",
        "module": "organizations",
        "action": "read"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: success
    request:
      url: "{{BASE_URI}}/api/v1/organizations/:org/rbac/permissions"
      method: DELETE
      params:
        - name: org
          value: testorg
          type: path
      body:
        type: json
        data: |-
          {
          	"role": "member",
          	"module": "organizations",
          	"action": "read"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: date
          value: Mon, 22 Jun 2026 07:40:25 GMT
        - name: server
          value: uvicorn
        - name: content-length
          value: "58"
        - name: content-type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "Permission removed successfully.",
            "data": null
          }
