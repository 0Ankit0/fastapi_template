info:
  name: Get Organization
  type: http
  seq: 3

http:
  method: GET
  url: /v1/organizations/{org_id}
  params:
    - name: org_id
      value: ""
      type: path
      description: undefined
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
