info:
  name: Delete Organization
  type: http
  seq: 6

http:
  method: DELETE
  url: /v1/organizations/{org_id}
  params:
    - name: org_id
      value: ""
      type: path
      description: undefined
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
