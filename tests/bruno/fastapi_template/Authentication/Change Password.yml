info:
  name: Change Password
  type: http
  seq: 7

http:
  method: POST
  url: /api/v1/auth/change-password/
  body:
    type: json
    data: |-
      {
      	"current_password": "",
      	"new_password": "",
      	"confirm_password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
