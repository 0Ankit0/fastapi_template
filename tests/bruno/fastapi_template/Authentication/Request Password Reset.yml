info:
  name: Request Password Reset
  type: http
  seq: 5

http:
  method: POST
  url: /api/v1/auth/password-reset-request/
  body:
    type: json
    data: |-
      {
      	"email": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
