info:
  name: Refresh Token
  type: http
  seq: 4

http:
  method: POST
  url: /api/v1/auth/refresh/?set_cookie=
  params:
    - name: set_cookie
      value: ""
      type: query
      description: undefined
  body:
    type: json
    data: |-
      {
      	"refresh_token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
