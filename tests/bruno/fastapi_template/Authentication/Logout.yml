info:
  name: Logout
  type: http
  seq: 9

http:
  method: POST
  url: /api/v1/auth/logout/
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
